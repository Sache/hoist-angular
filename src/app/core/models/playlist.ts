export interface Playlist {
  id: number,
  name: string;
  public: boolean;
  description: string;
}

/* export class Playlist implements Playlist{
  name: string;
  public: boolean;
  description: string;
} */